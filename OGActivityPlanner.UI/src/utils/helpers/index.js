// Import the required helper classes.
import CookieHelper from './cookiehelper';
import StringHelper from './stringhelper';

/**
 * Bundle the CookieHelper and StringHelper classes and export them as named exports,
 * making them available for import as individual classes or as a bundled object.
 * @type {Object}
 */
export {
  CookieHelper,
  StringHelper,
};
